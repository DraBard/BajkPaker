locally:

docker build -t bajkpaker .

docker run -p 3000:3000 <image_name>      -> To open the port locally, i.e. docker is on port 3000 and the local machine has to open port 3000 as well to access the webpage

